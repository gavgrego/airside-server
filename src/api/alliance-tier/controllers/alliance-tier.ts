"use strict";

/**
 * alliance-tier controller
 */

import { factories } from "@strapi/strapi";
const { createCoreController } = factories;

export default createCoreController("api::alliance-tier.alliance-tier");
